# Database (local dev — Railway sets this automatically via Postgres plugin reference)
DATABASE_URL="postgresql://user@localhost:5432/cannoli_trainer"

# NextAuth.js v5
AUTH_SECRET=           # Generate with: npx auth secret (or openssl rand -base64 32)
AUTH_URL=              # http://localhost:3000 (dev) or https://cannoli.mattalldian.com (prod)

# Resend (Email provider for magic link auth)
AUTH_RESEND_KEY=       # API key from https://resend.com/api-keys
EMAIL_FROM=            # e.g., Cannoli Trainer <noreply@cannoli.mattalldian.com>

# Seed (optional)
SEED_ATHLETE_EMAIL=    # Email to assign to test athlete for auth testing (e.g., matt@example.com)

# Railway Production Notes:
# - DATABASE_URL: Set as ${{Postgres.DATABASE_URL}} reference variable (auto-resolved)
# - AUTH_SECRET: Set manually in Railway dashboard
# - AUTH_URL: Set to https://cannoli.mattalldian.com
# - AUTH_RESEND_KEY: Set manually when email is configured
# - Build command: npm run build (runs prisma generate && next build)
# - Start command: npm run start
